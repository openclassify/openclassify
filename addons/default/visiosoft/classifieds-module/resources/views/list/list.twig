{% extends layout('list') %}

{% block styles %}
    {{ asset_style("visiosoft.module.classifieds::css/list.css") }}
    {{ asset_style("visiosoft.module.classifieds::css/list-new.scss") }}
{% endblock %}

{% block content %}

    {% include "visiosoft.module.classifieds::list/partials/mobile-list" %}

    <div id="listingPage" class="d-none d-sm-block">

        {% include "visiosoft.module.classifieds::list/partials/breadcrumb" %}

        <div id="mainContainer" class="d-flex mb-5">

            {% include "visiosoft.module.classifieds::list/partials/list-filter" %}

            <div id="listingContainer" class="flex-fill ml-4">
                <div id="pageTitle" class="d-flex justify-content-between border-bottom pb-2 px-3 mb-3">
                    <p>
                        {{ trans('visiosoft.module.classifieds::field.search_classified_result_text', {
                            'count': classifieds.total,
                            'name': catText ? catText : '',
                        })|raw }}
                    </p>

                    <a href="javascript:void(0)" class="d-flex align-items-center fav-search-modal">
                        <span class="mr-2">{{ trans('visiosoft.module.classifieds::field.save_search') }}</span>
                        {{ img('visiosoft.module.classifieds::images/listing/save-search.svg').data|raw }}
                    </a>
                </div>

                {% if count(cFArray) %}
                    <div id="queryBar" class="px-3 border py-2 filter-tabs d-flex flex-wrap mb-3">
                        {% for customParam in cFArray %}
                            <div class="d-flex align-items-center">
                                <span class="mr-1">{{ customParam['name'] }}</span>
                                {% for customValue in customParam['value'] %}
                                    <a href="{{ customValue['removalLink'] }}"
                                       class="text-truncate d-flex align-items-center mr-1">
                                        <span class="mr-2">{{ customValue['name'] }}</span>
                                        <button class="border-0 d-flex align-items-center justify-content-center">
                                            {{ img('visiosoft.module.classifieds::images/close.svg').data|raw }}
                                        </button>
                                    </a>
                                {% endfor %}
                            </div>
                        {% endfor %}
                        <div class="d-flex align-items-center">
                            <a href="{{ url_route('classified_list_seo', [category.slug]) }}"
                               class="text-truncate d-flex align-items-center mr-1">
                                {{ trans('visiosoft.module.classifieds::field.clear_all') }}
                            </a>
                        </div>
                    </div>
                {% endif %}

                <div id="viewTypes" class="d-flex justify-content-end">

                    {{ addBlock('classifieds-list/partials/before-display-mode', {'topfields': topfields})|raw }}

                    {% include "visiosoft.module.classifieds::list/partials/display-mode" %}

                </div>

                {{ addBlock('classifieds-list/row',{'topfields':topfields,'selectRange':selectRange,'classifieds':classifieds, 'mainCats':mainCats|length})|raw }}

                <div id="listingWrapper" class="border rounded p-3">
                    {% block listContent %}
                        {% include "visiosoft.module.classifieds::list/partials/classifieds" %}
                    {% endblock %}

                    {% include "visiosoft.module.classifieds::list/partials/pagination" %}

                    {{ addBlock('classifieds-list/row-bottom')|raw }}

                </div>
            </div>
        </div>
    </div>

    {% include "visiosoft.module.classifieds::list/partials/filter-modal" %}
    {% include "visiosoft.module.classifieds::list/partials/sort-modal" %}
    {{ addBlock('list/partials/modal')|raw }}

    <script>
        var select_trans = "{{ trans('visiosoft.module.classifieds::field.pick_option') }}";
    </script>

    {{ asset_add("scripts.js", "visiosoft.module.classifieds::js/list.js") }}
    {{ asset_add("scripts.js", "visiosoft.module.classifieds::js/list-new.js") }}

{% endblock %}

{% block customjs %}
    {{ addBlock('classifieds-list/list', {'classified': classified})|raw }}
{% endblock %}
