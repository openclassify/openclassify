{{ asset_add("scripts.js", "streams::js/form/translations.js") }}

{% block content %}

    {{ form.open()|raw }}

    {{ view("streams::form/partials/heading", {'form': form}) }}

    {{ view("streams::form/partials/layout", {'form': form}) }}

    {{ view("streams::buttons/buttons", {'buttons': form.actions}) }}

    {% if form.buttons.count() %}
        {{ view("streams::buttons/buttons", {'buttons': form.buttons}) }}
    {% endif %}

    {{ form.close()|raw }}

{% endblock %}
