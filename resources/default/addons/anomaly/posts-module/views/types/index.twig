{% extends layout('posts') %}

{% block content %}
    <div id="posts">
        <div class="container">

            <header class="jumbotron text-center">

                <h1>{{ type.title }}</h1>

                {% if type.description %}
                    <p>{{ type.description|raw }}</p>
                {% endif %}

            </header>

            {% set posts = type.posts().live().paginate() %}

            {% include "anomaly.module.posts::posts/partials/posts" with {"posts": posts} %}

            {{ posts.links|raw }}

        </div>
    </div>
{% endblock %}
